/** Vector base map. OpenFreeMap needs no key. Mapbox is selected at runtime, never by baking a token into this file. */
export {
  MAPBOX_STYLE_DARK,
  MAPBOX_STYLE_LIGHT,
  OPENFREEMAP_STYLE_DARK,
  OPENFREEMAP_STYLE_LIGHT,
  OPENSKIMAP_ATTRIBUTION,
  styleFor,
} from "./map-styles";
